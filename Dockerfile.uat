FROM node:18-alpine

WORKDIR /app

# Copy package files
COPY package*.json ./
RUN npm install

# Copy source code
COPY . .

# Set environment variable
ENV VITE_API_URL=http://localhost:3000

EXPOSE 5173

# Run dev server instead of building
CMD ["npm", "run", "dev", "--", "--host"]
